# Copie para .env e preencha. O .env não vai para o git (ver .gitignore).
#
# Há dois modos de backend de dados, e você só precisa configurar UM:
#
#   1. Stack local (docker compose up) — nada a preencher aqui. O compose já
#      injeta POSTGREST_URL, STORAGE_DIR e SUPABASE_DB_URL apontando para os
#      containers `postgrest` e `db`. Pule direto para a seção "App".
#   2. Supabase hospedado — preencha a seção abaixo.
#
# Ver docs/09-docker.md.

# --- Modo local (só se você rodar a API FORA do compose, contra a stack local) ---
# POSTGREST_URL=http://localhost:3000
# POSTGREST_TOKEN=            # vazio = papel anônimo do PostgREST (só dev)
# STORAGE_DIR=./.dados/storage
# API_BASE_URL=http://localhost:8000
# SUPABASE_DB_URL=postgresql://postgres:postgres@localhost:5432/sas

# --- Modo Supabase hospedado ---
# Supabase — credenciais do projeto.
# As duas chaves vêm de https://supabase.com/dashboard/project/<id>/settings/api
SUPABASE_URL=https://<seu-projeto>.supabase.co
SUPABASE_ANON_KEY=<chave-publica-jwt>
SUPABASE_SERVICE_KEY=<chave-de-servico-jwt>   # usada só pelo backend, NUNCA expor no frontend

# Postgres do Supabase — usado pelo migration runner (scripts/migrate.py).
# Pegue em https://supabase.com/dashboard/project/<id>/settings/database
# Recomendado: "Connection string" do pooler em "Transaction mode" (porta 6543).
# Formato: postgresql://postgres.<ref>:<senha>@aws-0-<regiao>.pooler.supabase.com:6543/postgres
SUPABASE_DB_URL=postgresql://postgres.<ref>:<senha>@aws-0-<regiao>.pooler.supabase.com:6543/postgres

# Storage — Supabase Storage por default (S3-compatível).
# Se quiser usar AWS S3 mesmo, descomente e ajuste.
STORAGE_BUCKET=sas-uploads
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...

# CORS — origens permitidas (frontend em dev e em produção).
CORS_ALLOW_ORIGINS=http://localhost:5173,https://sas.vercel.app

# App.
APP_ENV=dev

# E-mail via AWS SES — motor de lembretes (P2, docs/12-plano-p2-motor-lembretes.md).
# Sem estas 4 preenchidas, agendar COM lembrete devolve 422; o resto não muda.
# O remetente (e, em sandbox, cada destinatário) precisa estar verificado no SES.
AWS_SES_REGIAO=us-east-1
AWS_SES_ACCESS_KEY_ID=
AWS_SES_SECRET_ACCESS_KEY=
EMAIL_REMETENTE=ylucas2003@gmail.com   # dev — o definitivo é caixa do colégio, antes de entrar no ar

# Rede de segurança de dev: preenchido, TODO e-mail vai pra este endereço
# (o destinatário real vira prefixo do assunto). Vazio em produção.
EMAIL_DESTINATARIO_TESTE=

# Lembrete de aluno (P3, docs/13-plano-p3-lembrete-aluno.md).
# LEMBRETE_ALUNO_ATIVO=false → a varredura da véspera não materializa nada.
LEMBRETE_ALUNO_ATIVO=false
LEMBRETE_ALUNO_HORA=18:00
EMAIL_ENVIOS_POR_SEGUNDO=5
LEMBRETES_ORCAMENTO_SEGUNDOS=240
EMAIL_TETO_DIARIO=150          # sandbox do SES corta em 200/24h — subir quando sair

# Bounces e complaints (SES → SNS → API). Sem isso o envio funciona, mas
# endereço inválido nunca sai da lista — e é assim que a conta é suspensa.
EMAIL_CONFIGURATION_SET=sas-lembretes
SES_SNS_TOPIC_ARN=
SES_WEBHOOK_TOKEN=             # token no path do endpoint público do SNS
LEMBRETE_TOKEN_SECRET=         # HMAC do link de descadastro; vazio = usa o do JWT

# Segredo do scheduler (AWS EventBridge → X-Scheduler-Secret). Ver infra/README.md.
SCHEDULER_SECRET=
